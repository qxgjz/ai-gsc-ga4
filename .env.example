# Path to a Google service account JSON file.
# The service account needs read access to each GSC property and GA4 property.
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Optional. Defaults are relative to the project root.
DATA_DIR=./data
REPORTS_DIR=./reports
